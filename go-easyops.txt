NOTES ON GOEASY OPS CODE

Package Context:

- Used to transfer across API boundries and processes request-scoped values ie deadlines and cancellation signals

- Context are taken and when used by a function will return a child
!! Vital to use CancelFunc() to cancel the child and its children !!

AUTH:
	- 
